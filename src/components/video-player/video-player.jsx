import React from "react";
import PropTypes from "prop-types";

export default VideoPlayer extends React.PureComponent {
  constructor(props) {
    super(props);

    this.state = {isPlaying: false}
  }
  render() {
    
  }
}